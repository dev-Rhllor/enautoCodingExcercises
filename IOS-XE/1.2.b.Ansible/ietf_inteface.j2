{
    "interface": {
        "name": "{{item.name}}",
        "description": "{{item.description}}",
        "type": "iana-if-type:softwareLoopback",
        "enabled": true,
        "ietf-ip:ipv4": {
            "address": [
                {
                    "ip": "{{item.ip_address}}",
                    "netmask": "{{item.mask}}"
                }
            ]
        }
    }
}